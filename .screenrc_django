source .screenrc_base

# Django windows
screen -t 'runserver' 0
stuff "workon blah; python manage.py runserver\015"

screen -t 'shell' 1
stuff "workon blah; python manage.py shell\015"

screen -t 'mysql' 2
stuff "mysql5 -u root -p \015"

screen -t
