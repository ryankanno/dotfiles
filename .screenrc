# Ryan Kanno <ryankanno@localkinegrinds.com># Follow me on twitter @ryankanno # http://bit.ly/jLtj# UTF-8 everywheredefutf8 on# skip introstartup_message off# default lines is 100, let's do something huge.comdefscrollback 10000# detach on disconnectautodetach on# use normal audible bellvbell off# restore window contents after using vi, etc.altscreen on# use backtick for prefix key instead of Ctrl-Aescape ``# make the shell in every window a login shellshell -$SHELL# hardstatus alwayslastline "%{= kw}%-w%{= BW}%n %t%{-}%+w %-= %1` %m/%d/%y %C:%s%a"hardstatus alwayslastline "%{= kw}[ %{= kb}%H%{= kw} ][%= %{= kw}%?%-Lw%?%{= kW}%n*%f %t%?%?%{= kw}%?%+Lw%?%?%= ][ %{r}%l%{w} ]%{w}[%{r} %d/%m/%y %C %A %{w}]%{w}"